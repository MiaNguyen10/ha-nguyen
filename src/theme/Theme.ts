import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        background: {
            default: "#fffaf4",
        },
        text: {
            primary: "#45474b",
            
        },
    }
})

export default theme;